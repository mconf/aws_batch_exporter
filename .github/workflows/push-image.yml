name: Push new tag

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  push-scan:
    uses: mconf/mconf-ci-jobs/.github/workflows/lb-push-scan-image.yml@main
    secrets: inherit
    with:
      runs-on: '["ubuntu-22.04"]'
